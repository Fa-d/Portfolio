import React from 'react';
import DataManager from './DataManager';

const ManageProjects: React.FC = () => {
  return <DataManager dataType="projects" title="Projects" />;
};

export default ManageProjects;
